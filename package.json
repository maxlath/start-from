{
  "name": "start-from",
  "version": "1.0.0",
  "description": "Command-line tool to ignore lines of a stream until a line matches a given pattern, typically to resume an operation from the point where it stopped",
  "main": "index.js",
  "bin": {
    "start-from": "index.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "stream",
    "resume",
    "from",
    "pattern"
  ],
  "author": "maxlath",
  "license": "MIT",
  "dependencies": {
    "split": "^1.0.0",
    "through": "^2.3.8"
  }
}
